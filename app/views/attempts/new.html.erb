<% if @quiz.nil? %>
  <p>This quiz is no longer available</p>
<% elsif @quiz.available_for_participant?(@participant) %>
  <%= render 'form' %>
<% else %>
  <p>You have spent all the possible attempts to answer this quiz</p>
<% end -%>
